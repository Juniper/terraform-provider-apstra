---
page_title: "apstra_datacenter_switching_zones Data Source - terraform-provider-apstra"
subcategory: "Reference Design: Datacenter"
description: |-
  This data source returns the IDs of Switching Zones within the specified Blueprint. All of the filter attributes are optional. Requires Apstra >=6.2.0.
---

# apstra_datacenter_switching_zones (Data Source)

This data source returns the IDs of Switching Zones within the specified Blueprint. All of the `filter` attributes are optional. Requires Apstra >=6.2.0.


## Example Usage

```terraform
# Without specifying no filter, a wide search is performed.
# All switching zones in the blueprint will match.
data "apstra_datacenter_switching_zones" "all" {
  blueprint_id = "05f9d3fc-671a-4efc-8e91-5ef87b2937d3"
}

# This example performs a very narrow search. Only one (or zero!)
# switching zones can match the resulting query.
data "apstra_datacenter_switching_zones" "rzs" {
  blueprint_id = "05f9d3fc-671a-4efc-8e91-5ef87b2937d3"
  filters = [
    { # all filter attributes are optional
      name = "customer_1"
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `blueprint_id` (String) Apstra Blueprint ID.

### Optional

- `filters` (Attributes List) List of filters used to select only desired node IDs. For a node to match a filter, all specified attributes must match (each attribute within a filter is AND-ed together). The returned node IDs represent the nodes matched by all of the filters together (filters are OR-ed together). (see [below for nested schema](#nestedatt--filters))

### Read-Only

- `graph_queries` (List of String) Graph datastore queries which performed the lookup based on supplied filters.
- `ids` (Set of String) Set of Switching Zone IDs

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Optional:

- `description` (String) Description displayed in the web UI.
- `mac_vrf_name` (String) MAC VRF name used on network devices and visible in the web UI.
- `name` (String) Switching Zone Name.
- `route_target` (String) Route Target unique to the MAC-VRF Switching Zone.
- `service_type` (String) MAC VRF Service Type
- `tags` (Set of String) Set of Tags. All tags supplied here are used to match the Switching Zone, but a matching Switching Zone may have additional tags not enumerated in this set.

Read-Only:

- `blueprint_id` (String) Not applicable in filter context. Ignore.
- `id` (String) Not applicable in filter context. Ignore.
